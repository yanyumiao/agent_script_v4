# log.md — 问题记录与决策

## 2026-08-28：H3 帧数上限与「视频短于音频」的定格问题

### 遇到的问题

1. **H3 直接崩溃**：`frames must align within the released 5..362 range`
   - 触发：shot_12 配音 15.36s × 24fps = 369 帧 > H3 上限 362 帧
   - 根因：H3 视频 VAE 时域压缩是固定步长，合法帧数只能取 `5+17k`（k≤21），最大 362（≈15.08s @24fps）；超过则 latent 形状对不上、位置编码超出训练窗口，运行时直接拒绝
2. **衍生风险——定格**：视频被钳到 15.08s 后若配音更长，`utils/compose.py` 用 `tpad=stop_mode=clone` 补尾帧（复制最后一帧定格）。超出 ~1s 观感「卡住」

### 已修复

- `models/h3.py`：`frames = min(frames, config.H3_MAX_FRAMES)`（新增 `config.H3_MAX_FRAMES = 362`），边界统一钳制
- 超短部分由 compose `tpad` 补尾帧对齐（已验证：video 15.083s / voice 15.36s → 补 0.28s）
- CLAUDE.md「H3 怪癖」已补这条坑

### 调研事实（真实数据）

- 实测 12 镜头：字数/秒 **均值 4.4、最慢 3.5（旁白）、最快 5.4（对白）**；超限仅 shot_12（56 字 / 3.6 字/s / 15.36s）
- **分镜 LLM 的 `duration` 预估字段不可信**（shot 1 估 5s 实际 13.28s），判断镜头长度须按台词字数算
- **Qwen3-TTS 无语速参数**（`generate_custom_voice` 无 `speed`；`supports_tts_batch` 对 speed≠1.0 返回 False）→ 音频层不能事后校正时长

### 两轮搜索的结论：不矛盾，回答的不是同一个问题

- **搜索 1（视频剪辑）**：画面已拍好/生成好、不能重生成时，处理「视频短于音频」的标准 = **冻帧 + 慢速 Ken Burns 推拉**（补救技术）
- **搜索 2（动画前制 / AI 生成管线）**：内容未生成、可控制镜头切分时，标准 = **分镜拆镜预防**——单句台词 ≤15~18 字、单镜 ≤8s（目标 4~6s）、对白装不下必须拆镜（补救/预防之分只取决于「画面能不能重新生成」）
- 两者是**预防（主）+ 补救（兜底）两层**，不冲突：生成类管线主打预防，Ken Burns 是剪辑层兜底

### 分层设计（业界共识 + 我们现状对照）

| 层 | 行业标准 | 现状 | 差距 |
|---|---|---|---|
| 分镜层（主） | 单句 ≤15~18 字；旁白 ≤40 字；装不下拆镜；每镜最多 1 句核心台词 | 只要求时长 2~5s，未卡字数 | 需补 |
| 音频层 | audio-first：先配音、锁实际时长 | 已做（voices 节点在 shots 前） | 一致 |
| H3 层 | 15s 单段生成上限 | 钳 362 帧（15.08s） | 一致 |
| 合成层 | 分段拼接 / 画面衔接兜底 | 静态 `tpad` | 需升级为 Ken Burns |

### 待办

- [ ] 分镜 prompt 加台词字数预算 + 拆镜规则（`prompts.py` + `graph/nodes.py` 校验）
- [ ] Ken Burns 智能结尾：合成层 >0.5s 超时用冻帧慢推替代静态定格（注意 zoompan 逐帧整像素卡顿，需先放大源帧）
